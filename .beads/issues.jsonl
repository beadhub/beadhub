{"id":"beadhub-3pq","title":"Restrict agent aliases to pre-registered BeadHub roles","description":"## Problem\n\nAgents have been spinning up with ad-hoc random aliases (e.g. worker-fj7o, attach-dev) that aren't pre-registered in BeadHub. This causes:\n- Lost chat memory — new aliases have no history\n- Coordination gaps — orchestrator can't track unregistered agents\n- Identity sprawl — dozens of dead aliases accumulate on the server\n\n## Standard Agent Team\n\nThe following three agents are the fixed team across ALL BeadHub projects:\n\n| Alias | Role | Description |\n|-------|------|-------------|\n| **ordis** | coordinator | Global orchestrator (Remote Control pod). Plans, assigns, reviews, unblocks. Reachable via Discord and Claude Code Remote Control. |\n| **neo** | developer | Worker agent. Implements features, fixes bugs, submits PRs. |\n| **hawk** | reviewer | QA/review agent. Reviews PRs, checks security, test coverage, code quality. |\n\n### Per-project registration\n\nAgents are scoped per-project in BeadHub. When setting up a new project, register all three:\n\n```bash\nbdh :init --alias ordis --role coordinator   # from the main repo\nbdh :add-worktree developer --alias neo      # creates worktree + registers\nbdh :add-worktree reviewer --alias hawk      # creates worktree + registers\n```\n\nThis ensures every project has the same team with consistent aliases, roles, and chat history.\n\n### Rules\n- Agents must ONLY assume one of the three pre-registered aliases (ordis, neo, hawk)\n- No ad-hoc aliases — no worker-xyz, no attach-dev, no task-specific names\n- Worker K8s Jobs must claim `neo` or `hawk` via `bdh :init --alias \u003cname\u003e`, not create new identities\n- If a project needs more workers, register them with the human first (e.g. adding a fourth agent)\n\n### Global orchestrator = coordinator per project\n- ordis (the Remote Control pod) assumes the coordinator role for each project\n- When ordis does `bdh :init` into a project, it becomes that project's coordinator\n- The bdh notify hook fires on every bdh command, so ordis automatically receives messages from neo and hawk\n- ordis is reachable from both Discord (via discord-bridge) and Claude Code Remote Control\n\n### Cleanup done\n- Deleted all old ad-hoc aliases: alice, bob, charlie, attach-dev, bridge-dev, test-dev, worker-fj7o\n- Registered ordis (coordinator), neo (developer), hawk (reviewer) for hq-beadhub\n- Updated all policy playbooks to reference ordis by alias\n\n### Remaining work\n- Update orchestrator pod entrypoint to use `--alias ordis` (see beadhub-4j4)\n- Update policy to enforce \"no ad-hoc aliases\" as an invariant\n- Document the bootstrap procedure for new projects","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-01T12:37:54.100727409-05:00","created_by":"woodson","updated_at":"2026-03-01T14:32:57.955053454-05:00","closed_at":"2026-03-01T14:32:57.955053454-05:00","close_reason":"Implemented: CLAUDE.md rules, policy invariants, homelab-k8s ConfigMap/entrypoint updated, old agents deleted, ordis/neo/hawk registered"}
{"id":"beadhub-4j4","title":"Update orchestrator pod entrypoint to use alias ordis","description":"## Context\n\nThe orchestrator alias has been renamed from \"orchestrator\" to \"ordis\" in BeadHub. The orchestrator pod on the K8s cluster (managed by ArgoCD in homelab-k8s) still uses the old alias.\n\n## Requirements\n\n1. Update the orchestrator entrypoint in `Woody88/homelab-k8s` to use `bdh :init --alias ordis --role coordinator`\n2. Ensure the ConfigMap or CLAUDE.md mounted in the pod reflects the new alias\n3. The pod must register as \"ordis\" so Discord messages routed to the coordinator land on the right alias\n4. Verify the Remote Control session works after the rename\n\n## Important\n- This change must go through a commit to homelab-k8s (ArgoCD syncs from Git)\n- Do NOT kubectl patch/apply the orchestrator Deployment directly","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-01T13:08:30.094753382-05:00","created_by":"woodson","updated_at":"2026-03-01T14:32:57.957454127-05:00","closed_at":"2026-03-01T14:32:57.957454127-05:00","close_reason":"Implemented: CLAUDE.md rules, policy invariants, homelab-k8s ConfigMap/entrypoint updated, old agents deleted, ordis/neo/hawk registered"}
{"id":"beadhub-50q","title":"Ordis Control Plane Architecture","description":"# Ordis Control Plane Architecture\n\n## Overview\n\nOrdis (coordinator) runs as a project-agnostic control plane in a K8s pod on a Raspberry Pi 5.\nUsers interact via Discord #ordis channel. Ordis has full tool use (claude -p --dangerously-skip-permissions)\nand can spin up neo/hawk workers as K8s Jobs.\n\n## Components\n\n### 1. Central Control-Plane Project (BeadHub)\n- **Project slug:** `control-plane`\n- **Project ID:** `57dc7ce5-4722-480e-a92c-a29b50ef41bb`\n- **Ordis workspace_id:** `953d4875-f5c7-492f-971b-0918222601b1`\n- **Ordis API key:** stored as env var `ORDIS_API_KEY`\n- All Discord→ordis messages land here regardless of which project they're about\n\n### 2. Discord Interface\n- **Channel:** `#ordis` (ID: `1478072914543775824`)\n- **Webhook:** configured for the ordis channel\n- Single flat channel (no threads) for all communication with ordis\n\n### 3. Discord Bridge\n- Routes `#ordis` channel messages → BeadHub chat (control-plane project) via `CONTROL_PLANE_API_KEY`\n- Subscribes to Redis events → relays ordis responses back to `#ordis` via webhook\n- Uses Bearer auth (`CONTROL_PLANE_API_KEY`) for control-plane message fetching (not HMAC)\n- Source: `discord-bridge/src/` in hq-beadhub\n\n### 4. Orchestrator Pod (K8s)\n\n**Entrypoint flow:**\n1. Clone hq-beadhub (ordis's knowledge base)\n2. `bdh :init` for control-plane project\n3. Create minimal `/home/node/ordis/` workspace with CLAUDE.md + .beadhub\n4. Post \"ordis online\" to Discord\n5. Enter message watch loop\n\n**Message watcher loop (bash):**\n1. Poll `GET /v1/chat/pending` with Bearer auth (every 5s)\n2. Filter to sessions where `last_from != \"ordis\"` (skip self-messages)\n3. Extract `last_message` from pending response (no history fetch needed)\n4. Run `claude -p \"\u003cmessage\u003e\" --dangerously-skip-permissions --continue`\n5. Send response via `POST /v1/chat/sessions/{id}/messages` with Bearer auth\n6. Claude maintains conversation context via `--continue`; BeadHub stores full history as backup for compaction recovery\n\n**Key design: bash handles all chat I/O, claude -p handles thinking + tool use.**\nThis avoids multi-step tool-call latency for message read/send operations.\n\n### 5. Worker Jobs (neo/hawk)\n\nOrdis can spin up neo (developer) or hawk (reviewer) as K8s Jobs:\n- Same image: `ghcr.io/woody88/claude-agent:latest`\n- Same auth: `claude-setup-token` + `gh-pat` secrets\n- Same resources: **3 CPU / 3Gi memory** (matches orchestrator)\n- Same setup flow: gh auth, bdh init, claude -p with --dangerously-skip-permissions\n- **One worker at a time on the Pi** — check `kubectl get jobs -n beadhub` before creating\n- Job template is in ordis's CLAUDE.md so it knows how to create them\n- `ttlSecondsAfterFinished: 300` auto-cleans finished jobs\n\n### 6. Authentication\n- **Claude auth:** `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token` (1-year validity, no refresh needed)\n- **BeadHub auth:** `ORDIS_API_KEY` (Bearer token) for chat API calls\n- **Discord:** webhook URL for posting to #ordis channel\n- **GitHub:** `GH_TOKEN` for repo access\n\n### 7. Resources\n- **Orchestrator:** 3 CPU / 3Gi memory (requests: 500m / 1Gi)\n- **Workers:** 3 CPU / 3Gi memory (requests: 500m / 1Gi)\n- **Pi 5 total:** 4 CPU / ~7Gi RAM — only run one worker at a time alongside orchestrator\n\n## Message Flow\n\n```\nUser posts in #ordis Discord channel\n       ↓\ndiscord-bridge → POST /v1/chat/sessions (Bearer: CONTROL_PLANE_API_KEY)\n       ↓\nBeadHub stores message in control-plane project chat\n       ↓\nmessage-watcher polls GET /v1/chat/pending (Bearer: ORDIS_API_KEY)\n       ↓ (detects last_from != \"ordis\")\nclaude -p \"\u003cmessage\u003e\" --continue --dangerously-skip-permissions\n       ↓ (claude runs tools, reads files, executes commands)\nmessage-watcher sends response via POST /v1/chat/sessions/{id}/messages\n       ↓\nRedis event fires (chat.message_sent)\n       ↓\ndiscord-bridge fetches message via Bearer auth → posts to #ordis channel\n       ↓\nUser sees response in Discord\n```\n\n## Key Design Decisions\n\n1. **Bash handles chat I/O, not claude** — Each claude tool-call round trip is 30-60s.\n   Reading/sending messages in bash is instant. Claude only generates responses + uses tools.\n2. **Direct HTTP API, not bdh CLI** — The bdh CLI can't resolve service-type workspaces\n   (discord-bridge). Direct curl with Bearer auth bypasses this limitation.\n3. **--continue for session persistence** — Claude maintains conversation context across messages.\n   BeadHub stores full history as backup for when Claude does context compaction.\n4. **No status hook (for now)** — The \"thinking...\" indicator spammed Discord too much.\n   Will revisit with a smarter approach (post once on start, once on finish).\n5. **Setup token over credentials.json** — 1-year validity, no refresh sidecar needed.\n6. **Worker Job template in CLAUDE.md** — Ordis knows how to create neo/hawk Jobs with\n   matching resource limits. No separate template files needed.\n\n## Repos\n\n| Repo | Contains |\n|------|----------|\n| `Woody88/hq-beadhub` | discord-bridge source, agent-image, beads, ordis knowledge base |\n| `Woody88/homelab-k8s` | K8s manifests (ArgoCD-managed): orchestrator, RBAC, bridge deployment |\n\n## Secrets (K8s)\n\n| Secret | Purpose |\n|--------|---------|\n| claude-setup-token | CLAUDE_CODE_OAUTH_TOKEN for claude -p |\n| gh-pat | GitHub PAT for repo access |\n| ordis-api-key | BeadHub API key for ordis (control-plane project) |","status":"closed","priority":4,"issue_type":"task","created_at":"2026-03-02T11:59:50.756207118-05:00","created_by":"woodson","updated_at":"2026-03-02T17:25:30.797527876-05:00","closed_at":"2026-03-02T17:25:30.797527876-05:00","close_reason":"Updated architecture doc to reflect current implementation: direct HTTP API for chat I/O, claude -p with full tool use + --continue for session persistence, worker Job template with 3CPU/3Gi matching orchestrator, Bearer auth instead of HMAC for control-plane.","labels":["architecture","documentation"]}
{"id":"beadhub-60i8","title":"Route bdh chat to global orchestrator via bridge relay","description":"## Problem\n\nThe global orchestrator (K8s BLPOP dispatcher) can't receive bdh chat messages from developer agents. When a developer sends `bdh :aweb chat send-and-wait orchestrator 'question'`, the message goes to BeadHub DB and Redis pub/sub, but nothing pushes it to `orchestrator:inbox`. The developer's send-and-wait times out.\n\n## Solution\n\n1. **Bridge relay**: discord-bridge detects chat events targeting 'orchestrator' and pushes them to `orchestrator:inbox`\n2. **Stateless chat handling**: Dispatcher runs fresh `claude -p` for each chat (not --resume). Context recovered from bdh (chat history, status, bead details)\n3. **Role enforcement**: Orchestrator always uses coordinator role via bdh :init; spawned agents use roles defined in bdh policy\n4. **Project context**: Bridge includes repo info from BeadHub API so orchestrator can clone and set up project context\n5. **Cleanup**: Delete broken orchestrator-listener Deployment\n\n## Repos touched\n- hq-beadhub: discord-bridge changes (redis-listener, types, config, beadhub-client)\n- homelab-k8s: orchestrator.yaml dispatcher + orchestrator-docs.yaml CLAUDE.md\n\n## Plan file\nSee /home/woodson/.claude/plans/purring-hopping-hanrahan.md","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-02-27T22:44:54.320192551-05:00","created_by":"woodson","updated_at":"2026-02-27T22:57:12.377324135-05:00","closed_at":"2026-02-27T22:57:12.377324135-05:00","close_reason":"Implemented chat-to-orchestrator relay in discord-bridge, added processChatMessage handler in dispatcher, updated orchestrator docs with chat handling/role enforcement/project querying, fixed worker-template role placeholder, deleted broken orchestrator-listener"}
{"id":"beadhub-dp5","title":"Stream ordis tool activity to Discord thread per message","description":"## Overview\n\nWhen a user sends a message in #ordis, the discord-bridge creates a Discord thread on that message. The orchestrator streams claude -p output via --output-format stream-json, parses tool_use events, and posts real-time tool activity to the thread via the ordis webhook. The final response goes back through BeadHub chat as before.\n\n## Why\n\nCurrently there's zero visibility into what ordis is doing after receiving a message. Users don't know if it's working, stuck, or crashed. This feature gives real-time progress by streaming tool activity (actual commands, file reads, searches) into a collapsible Discord thread.\n\n## Components\n\n### 1. Discord Bridge Changes\n- When routing a #ordis channel message to BeadHub, also create a Discord thread on that message\n- Store the thread ID in Redis keyed by the BeadHub chat session_id so the orchestrator can look it up\n- Redis key pattern: ordis:thread:\u003csession_id\u003e → discord_thread_id\n- When relaying ordis responses back from Redis events, post to #ordis main channel (not the thread)\n\n### 2. Orchestrator Message Watcher Changes  \n- After detecting a pending message, look up the Discord thread ID from Redis\n- Replace plain claude -p capture with: claude -p --output-format stream-json --verbose | stream parser\n- Stream parser reads JSON events line by line:\n  - content_block_start (type=tool_use): capture tool name\n  - content_block_delta (type=input_json_delta): accumulate tool input JSON\n  - content_block_stop: format + post tool summary to Discord thread via webhook\n  - content_block_delta (type=text_delta): accumulate final response text\n  - message_stop: post checkmark to thread\n- Post tool summaries to Discord thread using DISCORD_ORDIS_WEBHOOK_URL with threadId parameter\n- Smart formatting: show actual command for Bash, filename for Read/Write/Edit, pattern for Grep/Glob\n- Rate limiting: skip if last post was \u003c3s ago to avoid flooding\n\n### 3. Stream-JSON Event Types\nEvents are wrapped: {type: 'stream_event', event: {type: '...', ...}}\nKey event.type values:\n- message_start: new turn starting\n- content_block_start: tool_use or text block beginning (has content_block.name for tools)\n- content_block_delta: partial content (text_delta, input_json_delta)\n- content_block_stop: block complete (time to post tool summary)\n- message_delta: stop_reason\n- message_stop: turn complete\n- ping: keepalive (ignore)\n- error: API error\n\n### 4. Tool Summary Format\n- Bash: 🔧 `\u003cfirst line of command, max 100 chars\u003e`\n- Read: 📖 Reading \u003cfilename\u003e\n- Write: 📝 Writing \u003cfilename\u003e  \n- Edit: ✏️ Editing \u003cfilename\u003e\n- Grep: 🔍 Searching: \u003cpattern\u003e\n- Glob: 📂 Finding: \u003cpattern\u003e\n- Agent: 🤖 Agent: \u003cdescription\u003e\n- Other: ⚙️ \u003ctool_name\u003e\n\n### 5. Thread Cleanup\n- Thread stays around after completion (users can review tool history)\n- No auto-delete needed — Discord archives threads naturally\n\n## Files to Modify\n- discord-bridge/src/discord-listener.ts — create thread on #ordis message, store in Redis\n- discord-bridge/src/redis-listener.ts — no change needed (responses still go to main channel)\n- homelab-k8s orchestrator.yaml — rewrite message loop with stream-json parser\n\n## No New Hooks Needed\nThe stream-json approach replaces the deferred discord-status.sh PostToolUse hook entirely.","status":"in_progress","priority":1,"issue_type":"feature","created_at":"2026-03-02T17:50:18.53138593-05:00","created_by":"woodson","updated_at":"2026-03-02T17:50:22.867747211-05:00"}
{"id":"beadhub-exa","title":"Enforce server-only ticket sync — no local dolt DB","description":"## Problem\n\nAgents have been attempting to use local dolt databases for ticket operations, which fail with 'table not found: issues' errors (see bridge-dev's mail). Agents also risk syncing upstream fork tickets into the project.\n\n## Requirements\n\n### No local dolt\n- Agents must NEVER start or depend on a local dolt database for beads/ticket operations\n- All ticket operations go through `bdh` (BeadHub server API) or `bd` (git-backed JSONL)\n- If `bd list` or `bd show` fails, the agent should NOT try to fix dolt — it should use `bdh list` / `bdh show` instead\n\n### Ticket creation sync flow\nWhen an agent creates a new ticket:\n1. `bd create ...` (creates locally in git-backed JSONL)\n2. `bd sync` (pushes to git remote)\n3. `bdh sync` or equivalent server sync (ensures BeadHub server picks up the new ticket from git)\n\nGit is the source of truth. The server syncs FROM git, not the other way around.\n\n### No upstream contamination\n- This repo is a fork of beadhub/beadhub but the upstream remote has been removed\n- Agents must NEVER add an upstream remote\n- Agents must NEVER sync tickets from the upstream fork\n- Only tickets created in Woody88/hq-beadhub are valid\n- If upstream tickets appear in the server DB, they must be purged\n\n### Policy update needed\n- Add these rules to the project policy via `bdh :policy`\n- Add to CLAUDE.md under a \"Ticket Sync Rules\" section\n- Update the orchestrator system prompt in homelab-k8s","status":"closed","priority":1,"issue_type":"task","created_at":"2026-03-01T12:37:41.907053535-05:00","created_by":"woodson","updated_at":"2026-03-01T14:32:57.950120161-05:00","closed_at":"2026-03-01T14:32:57.950120161-05:00","close_reason":"Implemented: CLAUDE.md rules, policy invariants, homelab-k8s ConfigMap/entrypoint updated, old agents deleted, ordis/neo/hawk registered"}
{"id":"beadhub-fj7o","title":"Update orchestrator system prompt for project context switching via bdh","description":"The orchestrator should be able to bdh :init into any project and inherit full chat history. Update the system prompt to: (1) explain project switching, (2) guide brainstorm-to-project transitions, (3) clarify when to use ephemeral sessions vs bdh-backed persistence. Also update known projects list as new projects are added.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-26T23:52:03.008758937-05:00","created_by":"woodson","updated_at":"2026-02-27T12:40:17.779426863-05:00","closed_at":"2026-02-27T12:40:17.779426863-05:00","close_reason":"Completed by hand — system prompt already updated in homelab-k8s. Orchestrator incorrectly rewrote its own Deployment spec when attempting this task."}
{"id":"beadhub-hr46","title":"Add SSH access to orchestrator pod for ADB/emulator on desktop","description":"The orchestrator needs SSH access to the human's desktop machine to run ADB commands for headless Android builds. Requires: SSH key pair, network route from Pi 5 to desktop, ADB CLI in agent image. Enables: build -\u003e screenshot verify -\u003e loop workflow.","status":"open","priority":2,"issue_type":"feature","created_at":"2026-02-26T23:51:43.518566553-05:00","created_by":"woodson","updated_at":"2026-02-26T23:51:43.518566553-05:00"}
{"id":"beadhub-jbnm","title":"CI/CD: Auto-build and push Docker images on merge","description":"Currently building and pushing Docker images is manual: pull changes, run docker buildx, push to ghcr.io, then restart pods. This needs automation.\n\nImages:\n- ghcr.io/woody88/discord-bridge:main (from discord-bridge/)\n- ghcr.io/woody88/beadhub:main (from ./)\n- ghcr.io/woody88/claude-agent:latest (from agent-image/)\n\nRepo: Woody88/hq-beadhub\n\nRequirements:\n- GitHub Actions workflow that triggers on merge to main\n- Detect which Dockerfile contexts changed and only build those\n- Build for linux/arm64 (Pi 5 target)\n- Push to ghcr.io\n- Optionally trigger pod restart on the cluster (webhook or ArgoCD image updater)\n\nConsiderations:\n- arm64 builds are slow on GitHub Actions (QEMU emulation) — may need self-hosted runner or buildx cloud\n- ArgoCD Image Updater could watch ghcr.io tags and auto-update deployments","status":"open","priority":2,"issue_type":"task","created_at":"2026-02-27T14:49:05.623426237-05:00","created_by":"woodson","updated_at":"2026-02-27T14:49:05.623426237-05:00"}
{"id":"beadhub-jqhe","title":"Remove sitelink-orchestrator CronJob","description":"The general orchestrator Deployment replaces the project-scoped sitelink-orchestrator CronJob. Remove sitelink-orchestrator.yaml from homelab-k8s/manifests/platform/beadhub/ and update kustomization.yaml. The general orchestrator can bdh :init into any project on demand.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-26T23:51:32.526767283-05:00","created_by":"woodson","updated_at":"2026-03-01T12:19:09.811519416-05:00","closed_at":"2026-03-01T07:21:42.735911Z"}
{"id":"beadhub-m5x7","title":"Rebuild agent image with ADB CLI","description":"Add ADB (Android Debug Bridge) CLI to the claude-agent Docker image (ghcr.io/woody88/claude-agent:latest). Required for headless Android builds and screenshot verification loop. Rebuild and push for arm64.","status":"open","priority":2,"issue_type":"task","created_at":"2026-02-26T23:51:55.391077532-05:00","created_by":"woodson","updated_at":"2026-02-26T23:51:55.391077532-05:00"}
{"id":"beadhub-sd6p","title":"Add Discord attachment support to orchestrator→bridge pipeline","description":"Enable the orchestrator agent to send file attachments (screenshots, audio, etc.) to Discord threads.\n\n## Repo\nClone Woody88/hq-beadhub — the discord-bridge source is in discord-bridge/src/.\n\n## Current State\nThe pipeline is text-only: Claude stdout → Redis outbox (JSON with response string) → bridge posts text via Discord webhook.\n\n## Required Changes\n\n1. **Outbox message format** (types.ts): Add optional `attachments` field to OrchestratorOutboxMessage:\n   - Each attachment: `{ filename: string, path: string }` (file path on orchestrator pod)\n   - Alternative: base64-encoded content if files can't be shared via filesystem\n\n2. **Agent file convention**: Claude saves files to a known directory (e.g. /tmp/discord-attachments/) and the dispatcher includes them in the outbox message. The dispatcher script in the orchestrator Deployment manifest (homelab-k8s repo) needs to scan for attachments after Claude exits.\n\n3. **Bridge relay** (orchestrator-relay.ts): When outbox message has attachments, use `webhook.send({ content, files: [...] })` instead of plain text. Discord.js supports AttachmentBuilder for file uploads.\n\n4. **Cross-pod file transfer**: Since orchestrator and bridge run in different pods, files can't be shared via filesystem. Options:\n   - Base64-encode files in the Redis message (simplest, works for \u003c8MB)\n   - Shared PVC mount (more complex)\n   - Temporary presigned URL (overkill)\n\n## HIL (Human-in-the-Loop)\nAfter implementation, the worker MUST ask the human to verify in Discord that attachments display correctly before closing this bead. Send a test screenshot and a test text file to confirm.\n\n## Acceptance Criteria\n- [ ] Orchestrator can send image files (png/jpg) to Discord threads\n- [ ] Orchestrator can send text/log files to Discord threads  \n- [ ] Messages with attachments also include text content\n- [ ] Files \u003e8MB are gracefully rejected with an error message\n- [ ] Human confirms attachments render correctly in Discord\n## Status Note (2026-03-01)\n\nCode is implemented but attachment delivery to Discord threads is broken.\n\n**Bug:** Webhook file uploads to threads fail with Discord error 10003 (Unknown Channel).\n- Text-only messages post to threads successfully via webhook\n- When files are included, Discord rejects the request with \"Unknown Channel\"\n- Root cause: Discord enforces stricter channel validation for multipart (file) webhook requests vs JSON text requests\n- Affected path: sendAsAgent() in discord-sender.ts -\u003e webhook.send({ files, threadId })\n\n**Fix needed:** Send files via bot client directly rather than via webhook when targeting threads.\n## Status Note (2026-03-01)\n\nCode is implemented but attachment delivery to Discord threads is broken.\n\n**Bug:** Webhook file uploads to threads fail with Discord error 10003 (Unknown Channel).\n- Text-only messages post to threads successfully via webhook\n- When files are included, Discord rejects the request with \"Unknown Channel\"\n- Root cause: Discord enforces stricter channel validation for multipart (file) webhook requests vs JSON text requests\n- Affected path: sendAsAgent() in discord-sender.ts -\u003e webhook.send({ files, threadId })\n\n**Fix needed:** Send files via bot client directly rather than via webhook when targeting threads.","status":"open","priority":2,"issue_type":"feature","created_at":"2026-02-27T01:57:20.299825142-05:00","created_by":"woodson","updated_at":"2026-03-01T12:19:09.813560325-05:00"}
{"id":"beadhub-thh0","title":"Add Cloudflare access to orchestrator pod","description":"Give the orchestrator access to Cloudflare for tunnel management and DNS operations. Requires adding Cloudflare API token as a K8s secret and mounting it in the orchestrator Deployment.","status":"open","priority":2,"issue_type":"feature","created_at":"2026-02-26T23:51:49.342165301-05:00","created_by":"woodson","updated_at":"2026-02-26T23:51:49.342165301-05:00"}
